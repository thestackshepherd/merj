# merj
Merj — a calendar consolidation and translation layer. It pulls events from any number of scattered sources (ICS feeds, computed generators, scraped schedules) into one normalized store, then serves that data back out in whatever shape you need
